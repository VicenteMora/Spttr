<h1> These are your matches </h1>

<% @match.each do | match | %>
	
	<%= match.first_name %> 
	<%= match.gym%>
	<%= match.availability %>
	<%= match.fitness_level %>
	<%= match.over_18 %> <br>

	<% end %>